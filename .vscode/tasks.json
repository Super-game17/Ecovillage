{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build cmake before (Debug)",
            "command": "cmake --build build --config Debug --target test_file -j 4 --",
            "type": "shell",
            "problemMatcher": [
                "$msCompile"
            ],
            "group": "build"
        }
    ]
}